export interface TechEvent {
  id: string;
  eventName: string;
  date: string;
  sortDate: string;
  city: string;
  venue: string;
  category: string;
  shortDescription: string;
  websiteLink: string;
}
